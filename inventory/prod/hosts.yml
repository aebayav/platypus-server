---
# Production inventory example (fill in with real server details)
all:
  children:
    platypus:
      hosts:
        prod-server-1:
          ansible_host: 10.0.0.11
          ansible_user: deploy
